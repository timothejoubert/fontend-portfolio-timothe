import type { GetterTree } from 'vuex'
import RootState from '~/types/store'

export const getters: GetterTree<RootState, RootState> = {
  // projects: (state: RootState) => state.projectsData,
}

export default getters
